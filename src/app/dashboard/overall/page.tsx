import React from 'react';
import { AIUsage } from '../_components/ai-usage';

const page = () => {
    return (
        <div>
            <AIUsage></AIUsage>
        </div>
    );
};

export default page;